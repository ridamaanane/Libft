/*

#include "libft.h"

void	*ft_memmove(void *dest, const void *src, size_t n)
{
	char	*d;
	char	*s;
	int		i;

	d = dest;
	s = (char *) src;
	i = 0;
	if (d > s)
		while (n--)
		{
			d(n) = s(n);
		}
	else
		while (i < n)
		{
			d[i] = s[i];
			i++;
		}
}

*/